export default defineAppConfig({
  ui: {
    primary: 'dodger-blue',
    container: {
      constrained: 'max-w-2xl',
    },
    button: {
      variant: {
        link: 'underline hover:no-underline',
      },
    },
  },
})
